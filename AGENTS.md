# macro-longevity

An open-source Go toolkit and data engine for macro-nutrition, biochemistry, and
dietary strategies targeting biological age optimization and healthspan extension.

MIT licensed — Copyright (c) 2026 Richmond Goh.

## State

Greenfield project. No source code, config, or dependencies yet.

## Conventions (inferred from `.gitignore`)

- Language: **Go**
- No dependency manager lockfile yet (no `go.mod` / `go.sum`)
- `.env` files are gitignored — use one for local secrets
- Go workspace files (`go.work`, `go.work.sum`) are gitignored
- Standard Go build/test artifact ignores (`*.exe`, `*.dll`, `*.so`, `*.dylib`,
  `*.test`, coverage profiles)

## Developer workflow

Not yet established. When bootstrapping:

```sh
go mod init github.com/richmondgoh/macro-longevity
```
